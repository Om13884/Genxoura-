# Database
# For PostgreSQL (Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/genxora?schema=public"

# For Supabase (alternative)
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# n8n Webhook Integration
# Use the COMPLETE webhook URL from n8n (including the path)
# Example: http://localhost:5678/webhook-test/form-submission
# Or base URL only: http://localhost:5678 (will append /webhook/contact)
# IMPORTANT: Use hyphens (-) not spaces in the path!
NEXT_PUBLIC_N8N_WEBHOOK_BASE="http://localhost:5678/webhook-test/form-submission"

# OpenRouter AI Integration
# Get your API key from https://openrouter.ai
OPENROUTER_API_KEY="sk-or-v1-..."

# SMTP Configuration (for email alerts)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Site URL (for OpenRouter referrer)
NEXT_PUBLIC_SITE_URL="https://genxora.com"

# Vercel Environment
VERCEL_ENV="production"

# Optional: Google Sheets Service Account (JSON key as string)
# GOOGLE_SHEETS_SERVICE_ACCOUNT='{"type":"service_account",...}'

